version: '3.8'

services:
  # Nginx web server with Traefik labels for public access.
  # Public assets come from the image (built from Dockerfile.prod nginx stage),
  # not from a host bind-mount — the VPS checkout has no vendor/ or public/build.
  nginx:
    build:
      context: .
      dockerfile: Dockerfile.prod
      target: nginx
    image: lateralzr_nginx:prod
    container_name: lateralzr_nginx
    restart: unless-stopped
    volumes:
      - ./docker/nginx/prod.conf:/etc/nginx/conf.d/default.conf:ro
    depends_on:
      - app
    networks:
      - gonzalezrico_platform
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.lateralzr-api.rule=Host(`api.lateralzr.com`)"
      - "traefik.http.routers.lateralzr-api.entrypoints=websecure"
      - "traefik.http.routers.lateralzr-api.tls=true"
      - "traefik.http.routers.lateralzr-api.tls.certresolver=resolver0"
      - "traefik.http.services.lateralzr-api.loadbalancer.server.port=80"
      # HTTP to HTTPS redirect
      - "traefik.http.routers.lateralzr-api-http.rule=Host(`api.lateralzr.com`)"
      - "traefik.http.routers.lateralzr-api-http.entrypoints=web"
      - "traefik.http.routers.lateralzr-api-http.middlewares=redirect_http_to_https@docker"

  # PHP-FPM application container — code and vendor/ live in the image.
  # Only mount writable storage from the host.
  app:
    build:
      context: .
      dockerfile: Dockerfile.prod
      target: app
    image: lateralzr_app:prod
    container_name: lateralzr_app
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./storage:/var/www/html/storage
    networks:
      - gonzalezrico_platform

  # Queue worker for background jobs
  queue:
    image: lateralzr_app:prod
    container_name: lateralzr_queue
    restart: unless-stopped
    env_file:
      - .env
    command: php artisan queue:work --queue=default --tries=3 --timeout=300 --sleep=3 --max-jobs=1000
    volumes:
      - ./storage:/var/www/html/storage
    networks:
      - gonzalezrico_platform
    depends_on:
      - app

  # Scheduler for Laravel's cron tasks
  scheduler:
    image: lateralzr_app:prod
    container_name: lateralzr_scheduler
    restart: unless-stopped
    env_file:
      - .env
    command: php artisan schedule:work --verbose
    volumes:
      - ./storage:/var/www/html/storage
    networks:
      - gonzalezrico_platform
    depends_on:
      - app

networks:
  gonzalezrico_platform:
    external: true
