; PHP fatals/warnings must reach container stderr. The stock image leaves
; log_errors Off, which is why Filament 500s never appeared in docker logs
; or laravel.log when the file logger was unwritable.
log_errors = On
error_log = /proc/self/fd/2
display_errors = Off
display_startup_errors = Off
